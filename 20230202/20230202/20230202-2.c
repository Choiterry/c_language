#include<stdio.h>

typedef struct NODE
{
	int ndata;
	char* str;
	struct NODE* next;
}NODE;
NODE* g_phead = NULL; // 노드의 첫번째를 기록하기 위한 전역변수 선언

void printlist()
{

	printf("%d", )
}
int main()
{
	NODE list[5] = {0};
	

	return 0;
}